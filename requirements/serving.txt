# Runtime dependencies for the BentoML inference service (src/serving/,
# TASK-5.1). Environment B versions (torch>=2.5 stack) -- proven to load and
# run a real Environment-A-trained STARCOP checkpoint via mlflow.pytorch,
# see mlops-methane-detection-plan.md TASK-5.1's 2026-08-15 readiness audit
# and its live-validation notes. Not vendor/starcop/requirements.txt's
# pinned Environment-A (torch==1.13.1) versions -- those are for training,
# not serving.
torch>=2.5
lightning>=2.3
segmentation-models-pytorch
torchmetrics
kornia
omegaconf
mlflow
boto3
numpy
bentoml
# Never imported by src/serving/ itself -- required because
# mlflow.pytorch.load_model unpickles the checkpoint into a real
# vendor/starcop/starcop/models/model_module.py ModelModule instance, which
# imports wandb at module level (STARCOP's own training-era code, composition-
# only, never edited -- see mlops-methane-detection-plan.md decision 0).
# Masked during local `bentoml serve` validation because wandb is an
# incidental root pyproject.toml (Environment B) dependency there; the
# containerized Bento only installs from this file, so it wasn't caught until
# the actual deployed image failed with "No module named 'wandb'".
wandb
# Same category as wandb above: starcop.utils (imported by model_module.py)
# imports rasterio at module level, needed to reconstruct the pickled model
# class, never called by src/serving/'s own inference path.
rasterio
